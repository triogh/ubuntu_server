---
- name: Install Perl related pkgs
  become: true
  apt: name={{ item }} state=present
  with_items:
    - perl-doc
    - libcpanplus-perl
    - cpanminus
    - perltidy
    - html2text
    - libperl-critic-perl
    #
    # perl -d:ptkdb <script_name.pl>
    - libdevel-ptkdb-perl
    - perl-tk
