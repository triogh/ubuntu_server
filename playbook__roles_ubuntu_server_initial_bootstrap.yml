---
- name: Playbook to bootstrap ubuntu server as desktop
  hosts: localhost
  connection: local
  gather_facts: yes
  vars_files:
    - "{{ ansible_env.HOME }}/my_ansible_external_vars.yml" 
  roles:
    - role: common
